<%= render "shared/header", title: "New Plan" %>

<% if @plan.days.blank? %>
  <%= render "initialize_days_form", plan: @plan %>
<% else %>
  <%= render "form", plan: @plan, exercises: @exercises %>
<% end %>

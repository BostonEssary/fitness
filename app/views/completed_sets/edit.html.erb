<%= turbo_frame_tag dom_id(@completed_set) do %>
  <div id="<%= dom_id(@completed_set, :content) %>" data-turbo-transition="slide-left" >
    <%= render 'form', completed_set: @completed_set, entry: @completed_set.entry, order: @completed_set.order %>
  </div>
<% end %>